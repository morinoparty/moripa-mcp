# Local dev (wrangler dev reads .dev.vars — never commit it)
SERVERS=main,res,lobby
MINEAUTH_BASE_URL=https://api.morino.party
MINEAUTH_SERVICE_TOKEN=CHANGE_ME
BEARER_TOKEN=CHANGE_ME
# Optional full override (e.g. local mock):
# MC_SERVERS_JSON={"local":{"baseUrl":"http://localhost:8080"}}
